function showHide() {
    var keyboard = document.getElementById("big-keyboard");

    if (keyboard.className === "show") {
        keyboard.className = "hide";
    } else {
        keyboard.className = "show";
    }
}

